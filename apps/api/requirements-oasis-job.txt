# Isolated OASIS social-research runtime.  OASIS 0.2.5 itself is installed
# from the commit- and SHA-pinned archive in Dockerfile.oasis after this
# hash-locked dependency set.  It requires Python >=3.10,<3.12, so this file
# must never be included by the API or native-runner images.
pandas==2.2.2
igraph==0.11.6
cairocffi==1.7.1
pillow==10.3.0
unstructured==0.13.7
sentence-transformers==3.0.0
prance==23.6.21.0
openapi-spec-validator==0.7.1
slack_sdk==3.31.0
neo4j==5.23.0
camel-ai==0.2.78
# The OASIS 0.2.5 release's own Poetry lock resolves CAMEL's broad
# ``mcp>=1.3`` range to 1.9.4.  Newer MCP releases removed FastMCP from the
# import path used by CAMEL 0.2.78, so do not allow resolver drift here.
mcp==1.9.4
requests_oauthlib==2.0.0
